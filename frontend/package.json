{
  "name": "project-root",
  "private": true,
  "scripts": {
    "start:backend": "cd backend && npm install && npm run dev",
    "start:frontend": "cd frontend/my-auth-app && npm install && npm run dev",
    "start": "concurrently \"npm: start:backend\" \"npm: start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}